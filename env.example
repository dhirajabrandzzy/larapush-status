# GitHub Webhook Configuration
# Generate a secret key for your webhook
# Use: openssl rand -hex 20
GITHUB_WEBHOOK_SECRET=your_generated_webhook_secret_here

# Note: Copy this file to .env and update the values
# Make sure .env file is writable only by the web server user
